name: arynox_settings
description: Arynox OS - System Settings
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  provider: ^6.1.1
  json_annotation: ^4.8.1
  shared_preferences: ^2.2.2
  dbus: ^0.7.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2
