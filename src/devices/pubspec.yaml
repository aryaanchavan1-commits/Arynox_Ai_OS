name: arynox_device_manager
description: Arynox OS Device Manager - Real-time device detection and management
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dbus: ^0.7.10
  provider: ^6.1.2
  flutter_glassmorphism: ^0.1.1
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
